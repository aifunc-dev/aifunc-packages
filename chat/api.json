{
  "version": "1.0.0",
  "name": "chat",
  "description": "Send a message and get a plain-text reply. Optionally include context such as prior turns.",
  "input": {
    "type": "object",
    "properties": {
      "message": {
        "type": "string",
        "description": "The user message.",
        "minLength": 1
      },
      "context": {
        "type": "string",
        "description": "Optional conversation history or other background text the reply should take into account."
      }
    },
    "required": ["message"],
    "additionalProperties": false
  },
  "output": {
    "type": "string",
    "description": "The assistant reply as plain text."
  },
  "errors": [
    { "code": "INVALID_INPUT", "message": "Input does not conform to the schema." },
    { "code": "MODEL_UNAVAILABLE", "message": "Model is unavailable." },
    { "code": "TIMEOUT", "message": "Request timed out." }
  ]
}
